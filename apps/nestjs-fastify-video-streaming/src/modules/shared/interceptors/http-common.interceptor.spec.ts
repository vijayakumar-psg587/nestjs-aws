import { HttpCommonInterceptor } from './http-common.interceptor';

describe('HttpCommonInterceptor', () => {
  it('should be defined', () => {
    expect(new HttpCommonInterceptor()).toBeDefined();
  });
});
